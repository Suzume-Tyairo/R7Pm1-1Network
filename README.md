令和7年度 春期 ネットワークスペシャリスト試験 午後1 問1 のネットワークです。

ルータR10の広域イーサ網側インタフェースにOSPFのコスト1001を設定すれば
PCからSaaSへの通信はPC → L2SW11 → L3SW10 → R11 → R31 → L2SW30 → SaaSとなると思っていますが
そうならずPC → L2SW11 → L3SW10 → R10 → R30 → L2SW30 → SaaSとなります。

(GNS3 2.2.55を使用してシミュレーション)
